`mission-orchestration.md` updated. Key changes from v2.3c reflected: restored `running → blocked`/`needs_decision` transitions, malformed-result handling, `late_result` classification, Commons Phase 1 in-process validation model, `attempt_id`/`workflow_run_id` provenance, and Drive non-canonical ratification. `source_commit_max` set to `c7df98632ed6dfee99daaada103cd613dc630501` for caller substitution.
