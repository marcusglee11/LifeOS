# Preflight Allowlist Configuration
# Canonical source of truth for patch file path validation (RPPV-002)
# Used by both validator and patch generator

allowlist:
  # Runtime orchestration
  - "runtime/orchestration/"
  # Runtime governance
  - "runtime/governance/"
  # Runtime tests
  - "runtime/tests/"
  # Scripts
  - "scripts/"
  # Configuration
  - "config/"
  # Documentation (non-governance sections)
  - "docs/02_protocols/"
  - "docs/03_runtime/"
  - "docs/11_admin/"

