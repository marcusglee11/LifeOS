---
description: Create a new isolated worktree branch with a topic-first command (defaults to build; supports fix/hotfix/spike).
---

Invoke the new-build skill and follow it exactly as presented.
If scoped work already exists in the primary tree, recover it with:
`python3 scripts/workflow/start_build.py --recover-primary`.
