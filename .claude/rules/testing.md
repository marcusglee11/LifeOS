---
paths: ["runtime/**", "scripts/**"]
---
# Testing

- Run `pytest runtime/tests -q` before AND after making changes.
- When adding new functionality, write the test first.
- When fixing bugs, add a regression test.
- Never commit with failing tests.
